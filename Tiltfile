local_resource(
    'install',
    cmd='bun i',
)


local_resource(
  "build",
  "bun turbo build",
  auto_init=False,
  trigger_mode=TRIGGER_MODE_MANUAL
)


local_resource(
    'dev',
    serve_cmd='bun turbo dev',
)
